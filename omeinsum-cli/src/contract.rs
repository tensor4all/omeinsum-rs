/// Run the contract subcommand.
pub fn run(
    _tensors_path: &str,
    _topology_path: Option<&str>,
    _expr: Option<&str>,
    _output: Option<&str>,
    _pretty: Option<bool>,
) -> Result<(), String> {
    Err("contract: not yet implemented".to_string())
}
